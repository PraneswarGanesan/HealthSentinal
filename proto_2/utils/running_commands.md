## Create ENv file
we should be having the .env files that should look like that for all the dependencies 

```
SUPABASE_URL
SUPABASE_KEY
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
AWS_REGION
S3_BUCKET
REDIS_URL
JWT_SECRET
JWT_EXPIRATION
OLLAMA_URL
```
## To run the application
```
python -m utils.test_utils
```
this is will test all the files are loaded correctly or not 
